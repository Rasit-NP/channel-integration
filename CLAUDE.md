# CLAUDE.md

이 문서는 Claude Code가 이 저장소에서 작업할 때 참조하는 프로젝트 지침입니다.

## 프로젝트 개요

여러 외부 숙박 상품 공급사(Supplier)의 서로 다른 API를 하나의 일관된 숙박 상품 모델로 통합하는 연동 백엔드입니다. 공급사마다 상품 표현 방식(요금 구조, 세금 처리, 실패 통지 방식)이 다르기 때문에, 이를 흡수하여 고객에게는 출처와 무관하게 동일한 형태의 검색 결과를 제공하는 것이 이 시스템의 핵심 목표입니다.

핵심 흐름은 다음과 같습니다.

1. 각 공급사의 숙소 목록 API를 호출해, 공급사 코드와 내부 식별자의 매핑을 미리 확보한다.
2. 고객의 검색 요청(날짜·인원)이 들어오면, 보유 숙소를 공급사별 코드로 묶어 재고·요금 API를 병렬 조회한다.
3. 각 공급사 응답을 내부 표준 모델로 정규화하고, 병합하여 통합 검색 결과를 반환한다. 일부 공급사가 실패해도 나머지 결과로 응답한다.

## 기술 스택

| 항목 | 선택 |
| --- | --- |
| Language | Java 21 |
| Framework | Spring Boot 3.5.x |
| Build | Gradle (Kotlin DSL) |
| Database | 관계형 DB (공급사 코드 ↔ 내부 식별자 매핑 저장) |
| HTTP Client | Spring WebClient (외부 공급사 연동 전용) |

- 외부 공급사 호출은 반드시 **WebClient**를 사용한다. RestClient·RestTemplate은 사용하지 않는다.
- WebFlux 전면 도입은 하지 않는다. MVC 위에서 WebClient만 사용한다. 이 상태는 `application.yaml`의 `spring.main.web-application-type: servlet`으로 코드에 고정되어 있다.
- 위 항목은 주어진 조건이다. 문서에 선택의 근거처럼 서술하지 않는다.

## 프로젝트 레이아웃

Spring Boot 프로젝트는 **저장소 루트**에 있다. 별도 하위 디렉토리를 두지 않는다.

```
build.gradle.kts / settings.gradle.kts / gradlew
src/main/java/com/channel/integration/
src/main/resources/application.yaml
docs/          설계 문서
.local/        커밋하지 않는 개인 참고 자료 (gitignore)
```

## 아키텍처 원칙

- **공급사 경계 격리**: 각 공급사의 요청/응답 형식이 도메인 계층으로 새어 나가지 않도록 어댑터 계층에서 차단한다. 도메인 계층은 내부 표준 모델만 다룬다.
- **신규 공급사 확장성**: 새 공급사를 추가할 때 어댑터 구현체만 추가하면 되도록 인터페이스를 설계한다. 도메인·검색 로직은 수정 대상이 아니어야 한다.
- **실패를 전제로 한 설계**: 외부 연동은 실패한다. 타임아웃, 부분 실패 허용, 공급사별로 다른 실패 통지 방식의 통일을 기본으로 다룬다.
- **실패 판정 통일**: 어떤 공급사는 HTTP 상태 코드로, 어떤 공급사는 항상 성공 상태(HTTP 200)를 주면서 응답 본문의 코드로만 실패를 알린다. 어댑터에서 이를 동일한 실패로 정규화한다.

## 저장 정책

- 요금·재고의 원본은 외부에 있으므로 저장하지 않는다. 매번 조회한다.
- 공급사 코드와 내부 식별자의 매핑만 DB에 저장한다.
    - 숙소 매핑: (공급사, 공급사 숙소 코드) → 내부 숙소 식별자
    - 객실 타입 매핑: (공급사, 숙소 코드, 공급사 객실 타입 코드) → 내부 객실 타입 식별자
- 객실 타입 코드는 해당 숙소 안에서만 유일하므로, 객실 타입 매핑 키에는 숙소 코드가 함께 들어간다.
- 같은 공급사 상품이 다시 조회되면 항상 같은 내부 식별자가 나와야 한다.

## API 계약

통합 검색 엔드포인트:

```
GET /api/v1/stays/search?checkIn=2026-09-01&checkOut=2026-09-04&adults=2&children=0
```

검색 조건은 날짜와 인원뿐이다. 지역·키워드 필터는 다루지 않는다(공급사가 지역 정보를 주지 않는다). 조회 대상은 보유 숙소 전체다.

응답에는 최소한 다음이 담긴다: 내부 숙소 식별자·숙소명, 내부 객실 타입 식별자·객실 타입명, 최대 수용 인원, 요청 기간 전체를 예약할 수 있는 객실 수, 출처 공급사, 요금 정보, 그리고 일부 공급사 조회가 실패했다면 그 사실.

## 코딩 컨벤션

- 패키지는 계층/책임 단위로 구성한다(adapter, domain, application, api 등).
- 공급사별 어댑터는 공통 인터페이스를 구현하고, 공급사 고유 DTO는 어댑터 패키지 밖으로 노출하지 않는다.
- 외부 응답을 내부 모델로 변환하는 매핑 로직은 명시적으로 분리한다.
- 설계 문서에 적은 결정은 실제 코드에 반영되어야 한다. 문서와 코드가 어긋나지 않게 유지한다.

## 문서 역할

문서마다 답하는 질문이 다르다. 이 경계를 지키지 않으면 같은 내용이 여러 곳에 쌓인다.

| 문서 | 답하는 질문 |
| --- | --- |
| `README.md` | 이게 뭐고, 어떻게 실행하고, **무엇을 왜 그렇게 정했나** |
| `docs/*.md` | 그 결정이 **정확히 어떤 모양인가** (필드·스키마·절차·시퀀스) |
| `JOURNAL.md` | **어쩌다** 그렇게 됐나 (바뀐 판단, 시행착오, AI 활용 기록) |
| `CLAUDE.md` | 이 저장소에서 지킬 규칙 |

**중복 방지 규칙**: "왜"는 README에만, "정확히 어떻게"는 docs/에만, "어쩌다"는 JOURNAL에만 쓴다. 다른 문서의 내용이 필요하면 **설명하지 말고 가리킨다**.

- `docs/`에 일자별 기록을 두지 않는다. 그건 JOURNAL의 몫이다.
- JOURNAL에 결론을 다시 설명하지 않는다. 처음 판단이 끝까지 유지된 결정은 README를 가리키는 한 줄로 둔다.
- JOURNAL의 "열린 문제"는 날짜별로 쌓지 않는다. 해결되면 지우는 살아있는 목록이다.
- 코드를 바꾼 작업의 끝에는 README 구현 현황 표를 다시 본다. 문서와 코드가 어긋나기 가장 쉬운 자리다.

## 커밋 규칙

- 의미 있는 작업 단위로 자주 커밋한다. 고민의 과정이 드러나는 히스토리를 남긴다.
- 커밋 메시지는 무엇을·왜 바꿨는지 중심으로 작성한다.

## 공개 저장소 주의사항

- 이 저장소는 Public이다.
- 프로젝트를 설명할 때는 배경을 도메인 언어로만 기술한다.
- 특정 회사명은 저장소명·코드·문서·커밋 메시지 어디에도 넣지 않는다.
- 외부 공급사 스펙 원문을 그대로 옮겨 담지 않는다. 필요한 내용은 본인 말로 재구성한다.
- `.local/`은 커밋 대상이 아니다. 커밋 전 `git status`로 확인한다.

## 테스트 / 실행

- 외부 공급사는 실제로 호출하지 않는다. 스펙을 흉내내는 Mock을 별도 포트(예: 9090)로 띄운다.
- Mock은 정상 응답, 공급사 장애 응답, 무응답의 세 상황을 재현할 수 있어야 한다. 이 셋이 있어야 타임아웃·부분 실패 동작을 검증할 수 있다.
- Mock은 본 애플리케이션과 같은 포트에 두지 않는다.
