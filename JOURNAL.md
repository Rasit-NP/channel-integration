# 진행 기록

설계 판단과 그 과정, 검토했다 버린 대안, AI 활용 방식을 남긴다.
결정의 **결과**는 README 에, **왜 그렇게 됐는지의 과정**은 여기에 적는다.

---

## Day 1 — 프로젝트 세팅과 설계 결정

### 수행 내용

- Spring Boot 프로젝트 스캐폴딩, 의존성 정리
- 통합 상품 모델·재고 판정·부분 실패 표현 등 주요 설계 결정 확정 및 README 작성
- Mock Supplier 구성 (별도 포트 9090, 정상/장애/무응답 3모드)
- 표준 모델(domain)과 공급사 연동 경계(port) 정의, 순수 로직 단위 테스트
- 공급사 A 어댑터 — WebClient 연동, 실패 정규화, 타임아웃

### 의사결정

Java 21, Gradle, Spring Boot, WebClient, 관계형 DB 는 주어진 조건이라 판단할 여지가 없었다.
고른 게 아니라 받은 것이므로 여기에 적지 않는다. 이 절에는 **도메인을 어떻게 모델링할지에
대한 판단**만 적는다.

**통합 요금 모델: 숙박 기간 전체 총액(세금 포함)을 표준으로**

이번 설계에서 제일 오래 고민한 지점이다. 두 공급사의 요금 표현이 비대칭이다.
A 는 날짜별 단가를 세금 별도로 주고, B 는 기간 총액만 세금 포함으로 준다.

처음엔 **날짜별 단가를 표준으로** 잡으려 했다. 정보량이 더 많으니 그게 상위 호환이라고
생각했다. 그런데 B 의 총액을 날짜별로 쪼개려면 날짜 수로 나누는 수밖에 없고, 그건 없는
정보를 지어내는 것이다. 그 값이 고객에게 노출되면 실제 결제액과 어긋난다.

변환이 한 방향으로만 가능하다는 걸 확인하고 뒤집었다. A 의 날짜별 `(단가 + 세액)` 을 합치면
세금 포함 총액이 정확히 나오지만, 역방향은 불가능하다. **정보를 잃는 것보다 지어내는 것이
훨씬 나쁘다**는 기준으로 총액을 표준으로 삼고, 날짜별 내역과 세액은 선택 필드로 내렸다.
잃는 것은 명확하다 — B 상품은 부분 취소 환불액 계산이나 세금 분리 표시가 불가능하다.

**조식 포함 여부를 요금과 같은 층위에 둠**

예시 데이터에서 같은 객실인데 A 429,000 / B 452,000 으로 총액이 달랐다. 처음엔 단순 가격차로
보고 싼 쪽을 노출할까 생각했는데, 확인해보니 B 는 조식 포함이고 A 는 미포함이었다. 조건이
다른 상품이다. 가격만 비교하게 만들면 안 된다고 보고 `breakfastIncluded` 를 표준 필드로 올렸다.

**중복 상품 병합은 하지 않음**

A 와 B 가 같은 숙소를 취급해도 그걸 알려주는 공통 키가 없다. 숙소명으로 추정할 수는 있지만,
추정이 틀리면 다른 숙소를 하나로 합쳐 파는 사고가 된다. 잘못 합치는 비용이 안 합치는 비용보다
크다고 판단해서, 공급사가 다르면 내부 식별자도 다르게 부여하고 각각 노출하기로 했다.

**연박 재고: 날짜별 최솟값**

3박을 예약하려면 그 객실 타입을 3일 연속 확보해야 하므로 병목이 되는 하루가 전체를 결정한다.
합계나 평균으로 하면 잔여가 `2/0/4` 인 상품을 예약 가능으로 노출하게 된다.

0 인 상품은 응답에서 빼기로 했다. 검색 API 의 계약을 "예약 가능한 상품 목록"으로 정의했기
때문이다. 대신 몇 건을 제외했는지는 응답에 남긴다. 결과가 빈 이유가 "전부 만실"인지
"공급사 조회 실패"인지는 클라이언트가 구분할 수 있어야 한다.

**실패를 예외가 아니라 값으로**

부분 실패를 허용하려면 "A 는 성공, B 는 실패"를 한 자료구조에 담아 다음 단계로 넘겨야 한다.
예외로 다루면 호출부마다 try-catch 로 감싸 다시 값으로 되돌리는 코드가 생긴다. 그래서
어댑터는 어떤 경우에도 예외를 던지지 않고 `SupplierFetchResult` 를 돌려주기로 했다.

실패 사유는 enum 하나로 통일했다. 공급사마다 실패를 알리는 방식이 다르지만 그 차이는 어댑터
안에서 끝나야 한다. 도메인이 "A 는 상태 코드, B 는 본문 코드"를 알면 공급사가 늘 때마다
도메인이 바뀐다.

**포트 시그니처를 값에서 Mono 로 되돌렸다**

처음엔 `SupplierAdapter` 가 결과를 바로 반환하게 정의하고 커밋까지 했다. A 어댑터를 붙이기
직전에 뒤집었다. 값을 바로 돌려주면 여러 공급사를 동시에 호출하는 데 별도 스레드 풀이 필요하고,
응답을 기다리는 동안 스레드가 묶인다. 동시에 기다리는 시간이 이 시스템의 부하인데 그걸
스레드로 감당하는 셈이 된다.

구현체가 하나도 없던 시점이라 바꾸는 비용이 거의 없었다. **인터페이스는 구현이 붙기 전에
의심해 보는 게 싸다**는 걸 확인했다. 블로킹은 컨트롤러 경계 한 곳에서만 한다.

**어댑터는 형태를, application 은 정체를**

어댑터가 어디까지 책임질지 애매했다. 재고 판정과 내부 식별자 매핑까지 어댑터에서 할까 했는데
나눴다. 재고 판정은 요청 기간을 알아야 하고 규칙도 공급사가 아니라 우리 정책이다. 식별자
매핑은 DB 가 필요하다. 어댑터는 표현의 **형태**만 통일하고, **정체**는 application 이 확정한다.

50개 묶음도 같은 기준으로 갈랐다. 한계는 공급사의 제약이니 어댑터가 `maxBatchSize()` 로
선언만 하고, 나누는 일은 application 이 한다. 한계가 다른 공급사가 들어와도 검색 로직은
그대로다.

**Mock 은 별도 프로세스로**

같은 포트에 두면 자기 자신을 HTTP 로 호출하게 되어 스레드가 묶인다. 연동 문제로 오해하기 쉬운
실패가 생긴다. `mock-supplier` 서브프로젝트로 분리하고 9090 으로 띄웠다.

Mock 자체에는 시간을 쓰지 않기로 했다. 요청 파라미터를 무시하고 고정 응답을 주는 수준이다.
필요한 건 데이터 다양성이 아니라 **정상 / 장애 / 무응답 세 상황의 재현**이다. 이 셋이 없으면
타임아웃과 부분 실패가 실제로 동작하는지 보일 방법이 없다.

### 검증

Mock 의 세 모드가 실제로 재현되는지 curl 로 확인했다. 확인하고 싶었던 건 응답이 오느냐가
아니라 **두 공급사가 서로 다르게 실패하느냐**다. 이 차이를 재현하지 못하면 나중에 어댑터가
실패 판정을 통일했는지 검증할 방법이 없다.

| 모드 | 공급사 A | 공급사 B |
| --- | --- | --- |
| normal | `HTTP 200` | `HTTP 200` |
| error | `HTTP 503` + `SERVICE_UNAVAILABLE` | **`HTTP 200`** + `resultCode: E503` |
| no-response | 응답 없음 (클라이언트 3초 타임아웃 확인) | 동일 |

error 행이 이 Mock 을 만든 이유 그 자체다. B 는 장애인데 HTTP 200 을 준다. 상태 코드만 보고
성공으로 처리하면 장애를 정상 응답으로 오처리하게 된다.

처음엔 curl 로 수동 확인만 했는데, A 어댑터를 붙이면서 자동화된 테스트로 옮겼다.

**테스트를 두 층으로 나눴다.** 처음엔 응답을 갈아끼우는 단위 테스트만 쓰려 했다. 그런데 그
방식은 전송 계층을 지나가지 않아서 Netty 타임아웃이 실제로 걸리는지, 직렬화 경로가 맞는지를
알 수 없다. 설정한 값이 코드에 반영됐는지는 확인되지 않고 "내가 짠 대로 동작한다"만 확인된다.

- `SupplierAAdapterTest` — `ExchangeFunction` 을 갈아끼워 변환과 실패 분류를 본다. 빠르다.
- `SupplierAMockIntegrationTest` — Mock 을 실제로 띄우고 HTTP 로 호출한다. Netty 타임아웃,
  직렬화, Mock 모드 전환까지 지나간다.

이를 위해 mock-supplier 를 테스트 의존성으로 넣었는데, 그 과정에서 문제 하나를 발견했다.
두 모듈이 각자 클래스패스 루트에 `application.yaml` 을 두면 어느 쪽이 읽힐지가 클래스패스
순서에 좌우된다. 지금은 메인 것이 먼저 잡혀 문제가 없었겠지만 조용히 깨질 종류라, Mock 의
설정을 기본 프로퍼티로 옮기고 파일을 없앴다.

### 막힌 지점 / 시행착오

**Initializr 가 3.5.x 를 더 이상 제공하지 않았다.** 4.x 만 목록에 있어서 4.1.1 로 스캐폴딩을
받은 뒤 `build.gradle.kts` 에서 버전을 내렸다. 로컬에 gradle CLI 가 없어 wrapper 를 직접
생성할 수 없었기 때문에, wrapper 를 얻으려면 Initializr 를 거치는 게 가장 빨랐다.

**커밋을 어떻게 나눌지 고민했다.** 전부 한 덩어리로 커밋하면 어디까지가 스캐폴딩이고 어디부터
내가 만든 것인지 구분되지 않는다. `.gitignore` 정리와 프로젝트 구성을 나눠 커밋했다.

**문서에 스택 선택의 근거를 길게 적었다가 전부 걷어냈다.** Java 21, Gradle, Spring Boot,
WebClient, 관계형 DB 는 처음부터 주어진 조건이었는데, README 에 "왜 WebClient 인가"를
한 문단 써두고 있었다. 고를 수 없었던 것을 고른 것처럼 쓴 셈이라 읽는 사람을 오해시킨다.
진짜 판단이 있었던 자리(요금 표준, 재고 판정, 부분 실패 표현)만 남기고 지웠다.

### AI 활용

이 프로젝트는 Claude Code 를 붙여 진행하고 있다. 저장소에 `CLAUDE.md` 를 두고 기술 스택·아키텍처
원칙·저장 정책·공개 저장소 주의사항을 미리 지시해서, 매번 배경을 다시 설명하지 않아도 되게 했다.

**받아들인 것**

- 요금 표준을 총액으로 잡을 때 "변환이 한 방향으로만 가능하다"는 정리. 내가 날짜별을 표준으로
  잡으려던 걸 뒤집는 근거가 됐다.
- `spring.main.web-application-type: servlet` 으로 웹 스택을 고정하자는 제안. web 과 webflux 가
  클래스패스에 함께 있으면 웹 스택이 자동 판정되는데, 지금은 서블릿으로 떠도 나중에 의존성이
  바뀌면 조용히 리액티브로 넘어갈 수 있다. **문서에만 적어둔 결정은 지켜지지 않는다**는
  지적이 타당해서 코드에 못박았다.
- Mock 을 별도 서브프로젝트로 분리하는 구성. 어댑터보다 Mock 을 먼저 만들자는 순서 제안도
  받아들였다. 고장을 재현할 상대가 없으면 견고성 코드를 짜도 동작을 확인할 수 없다.

**수정한 것**

- 처음 만들어진 `.gitignore` 주석에 이 저장소에 들어가면 안 되는 표현이 섞여 있었다.
  푸시 전이라 히스토리를 다시 써서 지웠다. 공개 저장소에 올라가는 텍스트는 커밋 전에 전부
  훑어야 한다는 걸 확인했다.
- README 초안이 `docs/` 하위 문서로 링크를 걸어뒀는데 그 파일들이 존재하지 않았다. 깨진 링크가
  되므로 "문서 계획" 표로 바꾸고 링크를 뺐다.

**거부한 것 / 잡아낸 것**

- README 초안의 기술 선택 표에 `Database | H2` 가 실제 구성처럼 적혀 있었다. 확인해보니
  `build.gradle.kts` 에 H2 도 JPA 도 JDBC 도 없었다. 다른 줄(Java 21, Boot 3.5.16)은 전부
  실재하는 항목이라 H2 도 구성된 것처럼 읽히는 상태였다.

  **AI 가 쓴 문서를 그대로 받으면 이런 게 그대로 남는다.** 표에서 빼고 저장 정책 절에
  "H2 를 쓸 예정이나 아직 의존성 없음"으로 옮겼다. 매핑 테이블 스키마를 확정하면서 JPA 로 갈지
  JDBC 로 갈지 함께 정하려고 일부러 미뤄둔 상태인데, 그 맥락까지 문서에 적었다.

- 같은 검증에서 두 가지를 더 잡았다. 재고 0 상품을 제외한다고 써놓고 응답 예시에는 제외 건수를
  담는 필드가 없었던 것, 그리고 "8080 에서 서비스합니다"라고 적혀 있지만 실제로는 엔드포인트가
  없어 404 가 나는 것. 둘 다 고쳤다.

### 남은 작업 — 알려진 문제

Day 1 마감 점검에서 찾았다. 동작을 막지는 않지만 그대로 두면 나중에 원인을 찾기 어려워진다.

**1. 매핑 단계 예외가 `UNKNOWN` 으로 분류된다**

`FailureReason.MALFORMED_RESPONSE` 의 정의는 "응답은 왔으나 표준 모델로 변환할 수 없음"인데,
지금은 JSON 파싱 실패만 여기에 걸린다. 파싱 후 매핑에서 터지는 예외(통화 불일치, 음수 금액
등)는 `UNKNOWN` 이 된다. `HttpFailures.fromThrowable` 의 switch 가 `CodecException` 만 잡고
나머지를 default 로 흘리기 때문이다.

임시 테스트로 확인했다 — 음수 요금을 넣으면 `Optional[UNKNOWN]` 이 나온다. 결과가 실패라는
사실은 맞아서 지금 동작에는 문제가 없다. 다만 공급사별 실패 사유를 지표로 볼 때 "우리 변환이
못 따라간 것"과 "원인 불명"이 한 칸에 섞인다. 그 둘은 대응이 다르다.

→ 변환 실패를 매퍼 안에서 잡아 사유를 붙이거나, `fromThrowable` 에서 구분한다.

**2. 묶음 크기 초과 시 동기 예외를 던진다**

`SupplierAAdapter.fetchOffers` 가 `Mono` 를 반환하기 전에 `throw` 한다. 나중에 여러 공급사를
`Flux.merge` 로 묶어 호출하면, 조립 시점에 던져진 예외가 한 공급사가 아니라 **merge 전체를
무너뜨린다.** 부분 실패를 허용하겠다는 설계와 어긋난다.

호출부 버그이므로 빨리 터뜨리는 게 맞다는 관점도 있다. 다만 `Mono.error` 로 돌려주면 다른
실패와 똑같은 경로로 흘러가 한 공급사만 실패로 남는다. 병렬 호출을 붙일 때 함께 정한다.

**그 밖에 사소한 것** — README 에 `supplier.a.*` 설정 키 설명이 없다. macOS 에서 기동하면
Netty DNS 리졸버 경고가 ERROR 레벨로 찍힌다(localhost 만 쓰므로 기능엔 영향 없음).

### 다음

1. 매핑 저장(H2) + 검색 유스케이스 — A 만으로 수직 슬라이스를 끝까지 관통시킨다.
   B 는 A 와 구조가 같아 붙는 게 거의 확실한 반면, 매핑 저장과 검색 조립에는 아직 드러나지
   않은 문제가 있을 수 있다. 파이프라인을 먼저 뚫어두면 B 는 그 위에 얹기만 하면 된다.
2. 공급사 B 어댑터. 도메인 코드를 안 고치고 붙는지가 경계 격리가 실제로 됐는지의 시험대다.
   B 는 일부러 까다롭다 — 장애에도 HTTP 200, 총액만 제공, 날짜별 요금 없음.
3. 병렬 호출과 부분 실패 병합. 위 남은 작업 2번을 여기서 함께 정리한다.
4. docs/ 정리 — 신규 공급사 추가 절차와 통합 모델 상세부터.
