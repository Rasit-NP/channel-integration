# 진행 기록

**무엇을 정했는지는 [README](README.md) 에 있다. 여기에는 그 결론에 이르는 동안 무엇이
바뀌었는지만 적는다.** 처음 생각과 끝이 같은 결정은 README 만 보면 되므로 여기 반복하지 않는다.
뒤집은 판단, 버린 대안, 잘못 짚었다가 고친 것이 이 문서의 내용이다.

## 요약

| 날짜 | 한 일 | 커밋 |
| --- | --- | --- |
| Day 1 | 프로젝트 세팅, 통합 모델 설계 확정, Mock 구성, 표준 모델·연동 경계, 공급사 A 어댑터, 문서 역할 정리 | `5092ae7` … `c8c45af` |
| Day 2 | 묶음 초과를 실패 값으로 정정, 매핑 저장 (H2 · JdbcClient), 숙소 목록 동기화, 통합 검색 API, 설계 문서 | `ec37898` … `34d53dd` |
| Day 3 | 전체 점검, 요금 총액이 요청 기간을 벗어나던 버그 수정, 묶음 크기 검증, 공급사 B 어댑터 | `8a5f712` … `46eb745` |
| Day 4 | 지표·모니터링 설계 문서. 쓰는 과정에서 연결 타임아웃 오분류를 찾아 고침 | `b0fda5f` … |

## 열린 문제

해결되면 지운다. 날짜별로 쌓지 않는다.

| # | 문제 | 방향 |
| --- | --- | --- |
| 1 | macOS 기동 시 Netty DNS 리졸버 경고가 ERROR 레벨로 찍힌다. localhost 만 쓰므로 기능엔 영향 없다 | 거슬리면 `netty-resolver-dns-native-macos` 를 개발 의존성으로 추가 |
| 2 | 무응답 모드 테스트가 끝나도 Mock 의 스레드가 10분간 붙잡혀 있어, 컨텍스트 종료가 30초 대기한 뒤 강제로 끊긴다. 빌드가 그만큼 길어진다 | 붙잡는 시간을 타임아웃보다 조금만 길게 줄이거나, 모드 복구 시 깨우는 방식으로 바꾼다 |
| 3 | 동기화가 공급사 목록을 한 건씩 돌며 저장한다. 숙소가 수천 개면 왕복이 그만큼 늘어난다 | 묶어 보내는 방식으로 바꾼다. 지금 데이터로는 걸리지 않아 미뤘다 |
| 4 | 어댑터가 못 옮겨 건너뛴 항목이 제외 카운터 셋 중 어디에도 안 잡힌다. "왜 비었는지 응답만으로 구분된다"는 계약에 네 번째 경로가 있는 셈이다 | 제외 사유를 하나 더 두거나, 어댑터가 건너뛴 수를 결과에 실어 올린다 |
| 5 | 컨트롤러의 `IllegalArgumentException` 핸들러가 너무 넓어, 검색 경로에서 새어 나온 예외도 400 + 내부 메시지로 나갈 수 있다 | 도메인 검증 실패와 그 밖의 예외를 갈라 잡는다 |
| 6 | 검색 결과 `stays` 에 정렬이 없어 같은 요청이 매번 다른 순서로 나간다. `api.md` 에 "순서 미정"으로 적어 두긴 했다 | 정렬 기준을 정하거나, 미정을 유지할 근거를 적는다 |
| 7 | `TIMEOUT` 하나에 성격이 다른 두 사건이 섞인다 — 묶음이 응답 제한을 넘긴 것(공급사가 느림)과 검색 전체 상한까지 안 온 것(우리 상한이 짧음). `detail` 로만 갈리는데 그건 지표 라벨에 못 넣는다 | 사유를 나누거나, 뒤쪽만 별도로 센다 |

---

## Day 1 — 프로젝트 세팅부터 공급사 A 어댑터까지

### 한 일

- Spring Boot 프로젝트 스캐폴딩, 의존성 정리
- 통합 상품 모델·재고 판정·부분 실패 표현 설계 확정 → README 작성
- Mock Supplier 구성 (별도 포트 9090, 정상/장애/무응답 3모드)
- 표준 모델(domain)과 공급사 연동 경계(port) 정의
- 공급사 A 어댑터 — WebClient 연동, 실패 정규화, 타임아웃. 테스트 37개

주어진 조건(Java 21, Gradle, Spring Boot, WebClient, 관계형 DB)은 고른 게 아니라 받은 것이므로
판단으로 적지 않는다.

### 판단이 바뀐 지점

**요금 표준을 날짜별 단가로 잡으려다 총액으로 뒤집었다** → README [§표준 요금](README.md)

처음엔 정보량이 많은 쪽이 상위 호환이라고 봤다. 뒤집은 계기는 **변환이 한 방향으로만 가능하다**는
걸 확인한 것이다. 날짜별을 합쳐 총액을 만들 수는 있어도, 총액을 날짜별로 쪼개려면 날짜 수로
나누는 수밖에 없고 그건 없는 값을 지어내는 일이다. 정보를 잃는 것보다 지어내는 것이 나쁘다는
기준이 이때 생겼고, 이후 판단에도 계속 썼다.

**포트 시그니처를 값에서 `Mono` 로 되돌렸다**

`SupplierAdapter` 가 결과를 바로 반환하도록 정의하고 **커밋까지 했다.** A 어댑터를 붙이기 직전에
뒤집었다. 값을 바로 돌려주면 여러 공급사를 동시에 호출하는 데 별도 스레드 풀이 필요하고,
응답을 기다리는 동안 스레드가 묶인다. 동시에 기다리는 시간이 이 시스템의 부하인데 그걸 스레드로
감당하는 셈이다.

구현체가 하나도 없던 시점이라 비용이 거의 없었다. **인터페이스는 구현이 붙기 전에 의심해 보는
게 싸다.**

**어댑터가 어디까지 책임질지 좁혔다**

재고 판정과 내부 식별자 매핑까지 어댑터에 넣으려다 뺐다. 재고 판정은 요청 기간을 알아야 하고
규칙도 공급사가 아니라 우리 정책이다. 매핑은 DB 가 필요하다. 어댑터는 표현의 **형태**만 통일하고
**정체**는 application 이 확정하는 것으로 갈랐다.

50개 묶음도 같은 기준으로 나눴다. 한계는 공급사의 제약이니 어댑터가 `maxBatchSize()` 로 선언만
하고, 나누는 일은 application 이 한다. 한계가 다른 공급사가 들어와도 검색 로직은 그대로다.

**테스트를 한 층만 쓰려다 두 층으로 나눴다**

응답을 갈아끼우는 단위 테스트면 충분하다고 봤는데, 그 방식은 전송 계층을 지나가지 않는다.
Netty 타임아웃이 실제로 걸리는지, 직렬화 경로가 맞는지는 확인되지 않고 "내가 짠 대로 동작한다"만
확인된다. Mock 을 실제로 띄우는 통합 테스트를 따로 뒀다.

**Mock 구성에 시간을 쓰려다 멈췄다**

WireMock 이냐 별도 모듈이냐를 고민했는데, Mock 은 결과물의 일부가 아니고 복사해 쓸 수 있는
뼈대가 있었다. 별도 포트로 띄우는 것만 지키고 나머지는 손대지 않았다. 요청 파라미터도 무시한다.
**필요한 건 데이터 다양성이 아니라 정상/장애/무응답 세 상황의 재현이다.**

### 그대로 간 결정

처음 판단이 끝까지 유지된 것들이다. 근거는 README 에 있다.

- 조식 포함 여부를 요금과 같은 층위의 표준 필드로 → README §조건이 다른 상품
- 중복 상품 병합하지 않음 (공통 키가 없어 추정에 의존해야 함) → README §동일 숙소를 병합하지 않는 이유
- 연박 재고는 날짜별 최솟값, 0 이면 응답에서 제외 → README §재고 판정
- 실패를 예외가 아니라 값으로. 부분 실패를 허용하려면 성공과 실패를 한 자료구조에 담아야 한다

### 막힌 것

**Initializr 가 3.5.x 를 제공하지 않았다.** 4.x 만 목록에 있어 4.1.1 로 받은 뒤 버전을 내렸다.
로컬에 gradle CLI 가 없어 wrapper 를 직접 만들 수 없었기 때문에 Initializr 를 거치는 게 빨랐다.

**두 모듈의 `application.yaml` 이 클래스패스 루트에서 충돌할 뻔했다.** Mock 을 테스트 의존성으로
넣으면서 발견했다. 어느 쪽이 읽힐지가 클래스패스 순서에 좌우된다. 지금은 메인 것이 먼저 잡혀
문제가 없었겠지만 조용히 깨질 종류라, Mock 설정을 기본 프로퍼티로 옮기고 파일을 없앴다.

**문서에 스택 선택의 근거를 길게 적었다가 전부 걷어냈다.** 주어진 조건을 고른 것처럼 쓴 셈이라
읽는 사람을 오해시킨다. 진짜 판단이 있었던 자리만 남겼다.

**커밋을 어떻게 나눌지 고민했다.** 한 덩어리로 커밋하면 어디까지가 스캐폴딩이고 어디부터 내가
만든 것인지 구분되지 않는다. 교체 전후를 각각 커밋해 diff 로 남기는 안도 검토했는데, 되돌렸다
다시 바꾸는 커밋을 인위적으로 만드는 셈이라 접었다.

### AI 활용

Claude Code 를 붙여 진행한다. 저장소에 `CLAUDE.md` 를 두고 기술 스택·아키텍처 원칙·저장 정책·
공개 저장소 주의사항을 미리 지시해, 매번 배경을 다시 설명하지 않아도 되게 했다.

**수용** — 요금 표준을 잡을 때 "변환이 한 방향으로만 가능하다"는 정리. 내가 날짜별을 표준으로
잡으려던 걸 뒤집는 근거가 됐다. `spring.main.web-application-type: servlet` 으로 웹 스택을
고정하자는 제안도 받아들였다. 문서에만 적어둔 결정은 지켜지지 않는다는 지적이 타당했다.
어댑터보다 Mock 을 먼저 만들자는 순서 제안도 마찬가지다 — 고장을 재현할 상대가 없으면 견고성
코드를 짜도 확인할 수 없다.

**수정** — 초기 `.gitignore` 주석에 이 저장소에 들어가면 안 되는 표현이 섞여 있었다. 푸시 전이라
히스토리를 다시 써서 지웠다. README 초안이 존재하지 않는 `docs/` 문서로 링크를 걸어둬서
"문서 계획" 표로 바꿨다.

**거부 / 잡아낸 것** — README 초안의 기술 스택 표에 `Database | H2` 가 실제 구성처럼 적혀 있었다.
확인해보니 `build.gradle.kts` 에 H2 도 JPA 도 JDBC 도 없었다. 다른 줄은 전부 실재하는 항목이라
H2 도 구성된 것처럼 읽히는 상태였다. **AI 가 쓴 문서를 그대로 받으면 이런 게 남는다.** 같은
검증에서 두 개를 더 잡았다 — 재고 0 을 제외한다고 써놓고 응답 예시에 제외 건수 필드가 없던 것,
"8080 에서 서비스합니다"라고 적었지만 실제로는 404 가 나는 것.

마감 점검에서도 구현 현황 표가 실제와 어긋난 걸 찾았다. 도메인 모델과 A 어댑터를 만들고도 표는
미구현으로 남아 있었다. **문서와 코드를 맞추겠다고 적어둔 표에서 같은 실수가 반복된다.** 작업
끝에 표를 다시 보는 것을 습관으로 삼는다.

### 다음

1. 매핑 저장(H2) + 검색 유스케이스 — A 만으로 수직 슬라이스를 끝까지 관통시킨다. B 는 A 와
   구조가 같아 붙는 게 거의 확실한 반면, 매핑 저장과 검색 조립에는 아직 드러나지 않은 문제가
   있을 수 있다
2. 공급사 B 어댑터. 도메인을 안 고치고 붙는지가 경계 격리의 시험대다. B 는 일부러 까다롭다 —
   장애에도 HTTP 200, 총액만 제공, 날짜별 요금 없음
3. 병렬 호출과 부분 실패 병합. 묶음 초과가 예외로 나가는 문제를 여기서 함께 정리한다
4. `docs/adding-supplier.md` — B 를 붙이면서 실제로 고친 것을 그대로 적는다

---

## Day 2 — 매핑 저장부터 통합 검색까지

### 판단이 바뀐 지점

**`Mono.error` 로 돌리려다 실패 값으로 갔다**

묶음 크기를 넘겼을 때 `SupplierAAdapter.fetchOffers` 가 `Mono` 를 만들기도 전에 예외를 던지고
있었다. 병렬 병합을 붙이면 한 공급사가 아니라 merge 전체가 무너지는 자리라, 열린 문제로 적어두고
`Mono.error` 로 바꿀 생각이었다.

바꾸려고 보니 `Mono.error` 도 반쪽이었다. 던지는 위치만 옮길 뿐 여전히 예외 채널로 나가고,
받는 쪽은 `onErrorResume` 으로 다시 값으로 되돌려야 한다. 포트가 "어떤 경우에도
`SupplierFetchResult` 를 돌려준다"고 선언해 둔 것과도 어긋난다. `FailureReason.INVALID_REQUEST`
주석에 "묶음 크기 초과 등"이라고 이미 적어둔 걸 보고, 자리가 처음부터 있었다는 걸 알았다.

**호출한 적도 없는데 실패로 치는 게 맞나**를 잠깐 의심했다. 맞다고 봤다. 실패 사유를 "공급사가
무엇을 돌려줬나"가 아니라 **"이 조회가 왜 결과를 못 냈나"**로 잡았기 때문이다. 잘못된 요청이라
보내지 않은 것도 그 조회가 결과를 못 낸 이유다. 응답에 드러나야 하는 것도 같다 — 고객 입장에서는
공급사가 죽은 것이나 우리가 잘못 물어본 것이나 **그 공급사 결과가 빠졌다**는 점에서 다르지 않다.

테스트도 함께 뒤집었다. 예외를 기대하던 것을 실패 사유를 확인하는 것으로 바꾸고, 호출하면
실패하는 `ExchangeFunction` 을 넣어 **정말로 호출하지 않는지**까지 같이 본다. 값으로 바꾸면서
"막았다"와 "불러봤다 실패했다"가 겉보기에 같아졌기 때문에, 그 구분은 테스트가 잡아야 한다.

### 미뤄둔 결정을 열어보니 정해진 게 아니었다

JPA 냐 JDBC 냐를 Day 1 에 미뤄두면서 "스키마를 정하면서 함께 결정한다"는 조건을 걸어뒀다.
꺼내기 전에 **주어진 조건에 정해진 게 있는지부터 확인했다.** 관계형 DB 를 쓴다는 것까지만
정해져 있고 접근 기술은 열려 있었다.

HTTP Client 가 WebClient 로 못박혀 있는 것과 대비된다. **못박을 항목은 못박혀 있다는 뜻이고,
DB 접근 방식이 열려 있는 건 우리가 정하라는 것으로 읽었다.** 그래서 고르는 것 자체보다 고른
이유를 남기는 쪽이 맞다고 보고 진행했다 → README [§무엇으로 저장하는가](README.md)

미뤄둔 조건도 결과적으로 맞았다. 스키마를 실제로 쓰기 전에는 "테이블 둘에 관계가 없다"가
얼마나 결정적인지 몰랐다. 그게 보이고 나니 ORM 을 얹을 이유가 남지 않았다.

### 스키마를 쓰다가 새로 생긴 질문

이름과 최대 수용 인원을 매핑 행에 같이 둘지 — 설계할 때는 나오지 않았던 질문이다. 컬럼을
적다가 걸렸다. 안 넣기로 했다 → README [§이름을 저장하지 않는 이유](README.md)

넣는 쪽 논거도 있었다. 공급사가 죽어 있어도 우리가 아는 숙소 목록을 보여줄 수 있고, 공급사
응답과 대조해 변경을 감지할 여지도 생긴다. 그런데 그 둘 다 **지금 없는 기능**이고, 대신
같은 값의 출처가 둘이 되는 문제는 **지금 생긴다.** 나중에 필요해지면 컬럼을 추가하는 쪽이,
지금부터 어긋남을 관리하는 것보다 싸다.

이건 "정보를 잃는 것보다 지어내는 것이 나쁘다"(Day 1)와는 다른 축이다. 여기서 잃는 건 없다.
**같은 사실을 두 곳에 두지 않는다**는 쪽에 가깝다.

### 리액티브 체인 안에서 JDBC 를 부를 뻔했다

동기화를 처음 짤 때 공급사 호출부터 저장까지 한 체인으로 이으려 했다. `fetchProperties()` 가
`Mono` 를 주니 그대로 `flatMap` 에서 저장까지 하면 깔끔해 보였다.

**JDBC 는 블로킹이다.** 그 저장은 리액터 스레드에서 돌고, 그 스레드는 다른 공급사의 응답도
기다리고 있다. 두 공급사를 동시에 부르려고 리액티브로 간 건데 저장 하나가 그 스레드를 붙잡으면
동시성이 그 자리에서 사라진다.

**호출은 병렬로, 저장은 순차로** 갈랐다. 응답을 다 모아 블로킹으로 넘어온 뒤 호출한 스레드에서
저장한다. 지금은 공급사가 둘뿐이라 성능 차이가 없지만, 경계를 지금 안 그으면 나중에 스레드
풀을 하나 더 만들어 덮게 된다.

포트 주석에 "블로킹은 컨트롤러 경계 한 곳에서만"이라고 써뒀는데, 동기화는 컨트롤러가 아니다.
말을 **바깥 경계**로 고쳤다. 기동·스케줄러·수동 트리거가 각각 그 경계다.

### 지우지 않기로 한 것

목록에서 사라진 코드를 지울지 — 설계할 때 나오지 않았던 질문이다. 동기화를 짜다가 걸렸다.
지우지 않기로 했다 → README [§목록에서 사라진 숙소를 지우지 않는 이유](README.md)

여기서도 기준은 Day 1 과 같았다. 공급사가 접은 것과 목록 API 가 빠뜨린 것을 **구분할 방법이
없는데**, 지우는 쪽은 그 둘을 같게 취급하겠다는 뜻이다. 모르는 것을 안다고 가정하는 자리가
또 나왔다.

### 제외 카운터를 하나로 두려다 셋으로 쪼갰다

검색 결과에서 빠지는 이유가 셋이라는 걸 짜면서 알았다 — 만실, 매핑에 없음, 인원 초과. 처음엔
`excluded` 하나로 합치려 했다. 필드가 늘어나는 게 부담스러웠다.

쪼갠 이유는 **셋이 서로 다른 사람의 할 일**이기 때문이다. 만실은 고객이 날짜를 바꾸면 되고,
인원 초과는 인원을 줄이면 된다. 그런데 **매핑에 없어서 뺀 것은 우리 문제다** — 동기화가 밀렸거나
공급사가 목록에 없던 걸 판다는 뜻이라 우리가 고쳐야 한다. 합쳐 놓으면 이 신호가 만실 뒤에
숨는다. 응답 필드 하나 아끼려다 우리 문제를 못 보게 되는 셈이었다 → README
[§무엇을 왜 뺐는지](README.md)

### 공급사가 계약을 지킨다고 가정하지 않기로 했다

스펙상 공급사는 요청 인원을 수용할 수 있는 객실 타입만 준다. 그러면 우리가 `maxOccupancy` 를
또 볼 이유가 없다 — 처음엔 그렇게 생각하고 필터를 넣지 않았다.

Mock 이 요청 파라미터를 무시하고 고정 응답을 준다는 걸 떠올리고 뒤집었다. Mock 이라 그런 거지만,
**실제 공급사도 계약을 어길 수 있다**는 게 요점이다. 어겼을 때 우리가 치르는 비용은 4명이 묵을
수 없는 방을 4명에게 파는 것이고, 검사 한 줄보다 훨씬 비싸다. 재고 날짜가 빠지면 0 으로 보기로
한 것과 같은 기조라, 여기서만 상대를 믿을 이유도 없었다.

### 늦은 것도 부분 실패로 다뤘다

어댑터의 응답 제한은 묶음 하나에 걸린다. 묶음이 많아지면 합은 그보다 길어지므로 검색 전체에도
상한을 뒀는데, **넘겼을 때 무엇을 돌려줄지**가 문제였다.

`block(timeout)` 은 넘기면 예외를 던진다. 그러면 이미 도착한 다른 공급사 결과까지 통째로
버린다. 부분 실패를 허용하기로 해놓고 늦은 경우에만 전부 버리는 건 앞뒤가 안 맞는다. 도착한
묶음까지만 받고(`take(duration)`) 못 받은 공급사를 타임아웃으로 표시하는 쪽으로 갔다. 공급사가
죽은 것과 늦은 것은 고객에게 같은 사건이다 — 그 공급사 결과가 빠졌다.

이러려면 **공급사마다 묶음이 몇 개인지 미리 알고 있어야** 한다. 몇 개 왔는지만 봐서는 덜 온 건지
원래 그만큼인지 모른다. 계획을 세우는 단계에서 묶음 수를 들고 있게 한 이유다.

### 테스트가 무엇을 지키는지

upsert 가 도는지가 아니라 **내부 식별자가 안정적인지**를 본다. 같은 목록을 두 번 넣고 식별자를
비교하는 테스트가 이 저장소의 존재 이유를 그대로 검사한다.

동기화 쪽도 같은 기준으로 골랐다. "공급사가 죽어도 애플리케이션은 뜬다"는 README 의 약속을
닿지 않는 주소를 물려 실제로 기동시켜 확인한다. **문서에만 적힌 약속은 지켜지지 않는다**는 걸
Day 1 에 이미 한 번 겪었다.

검색은 한 층 더 뒀다. 조각마다 단위 테스트가 있어도 **이어 붙였을 때 도는지는 보여주지
못한다.** Mock 을 진짜 HTTP 로 세우고 동기화부터 JSON 응답까지 관통시키는 테스트를 따로 뒀고,
거기서 확인하는 숫자는 README 에 적어둔 것과 같다 — 재고 3/1/5 면 1실, 총액 429,000원,
재고 0 이 낀 상품은 빠지고 `excludedSoldOut` 이 1.

unique 제약이 실재하는지도 따로 확인한다. 식별자 안정성을 보장하는 것은 upsert 문장이 아니라
제약인데, `MERGE` 만 통과시켜 보면 제약이 빠져 있어도 테스트가 통과한다. 보장 장치라고 문서에
써둔 것은 그것이 있는지를 직접 봐야 한다.

### AI 활용

**수용** — 남은 작업 순서를 짜면서, 이 수정을 매핑 저장보다 먼저 두자는 제안을 받아들였다.
병렬 병합을 붙인 뒤에 고치면 merge 가 무너지는 걸 디버깅하다 찾게 된다. 5분짜리를 앞으로
당기는 게 낫다.

**수정** — 열린 문제에 적어둔 `Mono.error` 를 그대로 하려다 위 이유로 실패 값으로 바꿨다.
문서–코드 대조에서 세 건을 받았고(포트 계약과 어긋나는 `throw`, 없는 `docs/` 디렉토리,
Day 1 커밋 범위 누락) 셋 다 실재해서 반영했다.

**되물어서 얻은 것** — JPA/JDBC 선택지를 정리해 준 답에 곧바로 고르지 않고 **"주어진 조건에
정해진 게 있느냐"를 먼저 물었다.** 없다는 것을 확인하고 나서야 고르는 게 순서였다. 정해진 것을
내 판단으로 덮어쓰는 일과, 열려 있는 것을 내가 정하는 일은 남겨야 할 기록이 다르다. 답을 받고
바로 고르면 그 확인이 통째로 빠진다.

---

## Day 3 — 재고는 기간을 아는데 요금은 몰랐다

### 무엇이 틀려 있었나

공급사 응답을 표준 모델로 옮길 때, **재고는 요청 기간으로 거르는데 요금은 거르지 않고 있었다.**
`Availability.forStay(재고, 기간)` 는 기간 밖 날짜를 버리는 반면, `StayPrice.fromNightlyRates(요금)`
는 기간을 아예 받지 않아 공급사가 준 날짜를 전부 더했다. 3박을 물었는데 공급사가 하루를 더 얹어
보내면 **하루치를 더 청구한다.** 임시 테스트로 확인했다 — 330,000원이어야 할 총액이 440,000원이었다.

**이게 왜 안 보였는가**가 더 중요하다. `sum(nightlyRates[].amount) == totalAmount` 는 그대로
성립한다. 내역과 총액이 서로 맞으니 응답만 봐서는 틀린 것을 알 수 없고, 재고 판정은 정상이라
"예약 가능한 상품이 제대로 나왔는데 금액만 다른" 모양이 된다.

### 어댑터가 아니라 도메인에 뒀다

세 갈래가 있었다. ① 매퍼에서 자른다 ② 요금 팩토리가 기간을 요구한다 ③ application 에서 나중에
고친다.

①이 변경은 가장 작다. 버린 이유는 **공급사마다 되풀이해야 하는 암묵적 규칙**이 되기 때문이다.
잊어도 컴파일이 되고 테스트도 안 깨진다. 하필 B 는 총액만 주므로 이 규칙이 필요 없어서, C·D 를
붙일 때 "안 하는 쪽"이 선례로 남는다. ③은 `SupplierOffer` 가 틀린 총액을 들고 다니다 뒤에서
교정되는 구조라 더 나쁘다.

②로 갔다. **기간 없이는 요금을 만들 수 없게 하면 새 어댑터가 빠뜨릴 방법이 없다.** 이 저장소가
경계를 지키는 다른 장치들(package-private DTO, sealed 결과 타입, unique 제약)과 같은 성격이다 —
규약이 아니라 컴파일러가 강제한다. 재고와 모양이 같아지는 것도 덤이다.

**B 를 붙이기 전이라 싼 변경이었다.** Day 1 에 포트 시그니처를 뒤집으며 적은 것과 같다 —
인터페이스는 구현이 붙기 전에 의심해 보는 게 싸다.

### 넘침과 모자람을 다르게 다뤘다

기간과 어긋나는 방향은 둘인데 처리는 대칭이 아니다 → README [§총액은 요청 기간에 대해서만
만듭니다](README.md)

기간 밖 날짜를 **버리는 것은 잃는 게 없다.** 숙박에 포함되지 않는 날이다. 반면 빠진 날의 요금을
**채우려면 지어내야 한다.** Day 1 의 "정보를 잃는 것보다 지어내는 것이 나쁘다"가 여기서도 답을
줬다. 넘침만 막고 끝냈으면 과소 청구가 그대로 남았을 텐데, **과소 청구는 고객이 항의하지 않아서
더 오래 남는다.**

날짜 집합이 요청 기간과 일치해야 한다는 한 규칙으로 묶으니 **중복 날짜 이중 합산까지 함께
걸렸다.** 따로 다뤘으면 검사가 셋이 됐다.

### 테스트 픽스처가 새 규칙과 부딪혔다

구현 전에 한 번 더 점검하다가 두 건을 찾았다. `skipsUnmappableItemsOnly` 에서 **살아남아야 하는**
항목이 3박 요청에 하루치 요금만 갖고 있었고, 컨트롤러 테스트 픽스처도 그랬다.

바로 짰으면 테스트가 깨진 뒤에 알았을 것이다. 그런데 이건 픽스처 손질로 끝날 일이 아니라
**같은 뿌리의 증상**이다 — 요청 기간과 맞지 않는 요금 데이터가 테스트에서도 아무 저항 없이
통과하고 있었다. 그래서 버그가 살아남았다. 검증하는 쪽이 같은 구멍을 갖고 있으면 잡을 수 없다.

### 카운터 하나를 내줬다

요금과 재고가 **같이** 모자란 상품은 지금까지 재고 판정에 걸려 `excludedSoldOut` 으로 세어졌다.
이제는 어댑터가 먼저 거부해 어느 카운터에도 안 잡힌다.

그래도 이쪽이 맞다고 봤다. 그 "만실" 딱지는 **사실과 다른 라벨**이었다. 재고가 없는 게 아니라
공급사가 요금을 덜 준 것인데, 고객에게는 "날짜를 바꾸세요"로 안내된다. 틀린 이름을 잃은 것이라,
제대로 된 이름은 열린 문제 5 에서 붙인다.

### 무한 루프가 될 자리를 두 층에서 막았다

묶음 크기 상한이 0 이면 나누는 반복이 끝나지 않는다. 설정값 하나로 요청 스레드가 멈추는 것이라
늦게 알수록 원인을 찾기 어렵다.

같은 검사를 두 곳에 뒀는데, **막는 층이 다르다.** 설정 쪽은 기동 때 못 뜨게 하고(`SearchProperties`
가 `maxConcurrency` 에 이미 쓰는 방식이다), `partition` 쪽은 어댑터 구현이 이상한 값을 선언해도
멈추지만은 않게 한다. 앞은 설정 오타를, 뒤는 포트 계약 위반을 막는다. 같은 규칙의 중복이 아니다.

검증을 `SupplierHttpProperties` 에 `static` 으로 둔 것은, 그 인터페이스가 `maxBatchSize()` 를
선언한 주인이기 때문이다. 공급사가 늘어도 각자 다시 정하지 않는다. **B 를 붙이며 바로 재사용된다.**

## Day 3 (이어서) — 공급사 B

### 경계가 지켜졌는지는 diff 로만 확인된다

B 를 붙이며 **`domain`·`port`·`application`·`api` 운영 코드는 한 줄도 바뀌지 않았다.** 늘어난
것은 `adapter/b/` 다섯 파일과 `application.yaml` 의 `supplier.b.*` 뿐이다.

이걸 미리 확인 기준으로 정해 두고 시작했다. 붙이고 나서 "잘 붙었다"고 쓰면 그건 소감이고,
`git diff --stat` 으로 0 줄을 보이면 사실이다. **경계 격리는 주장이 아니라 관측 대상이어야 한다.**

의미가 있으려면 흡수할 차이가 실제로 있어야 하는데, B 는 요금 표현도(총액만) 실패 통지 방식도
(200 + 본문 코드) A 와 다르다. 둘 다 어댑터 안에서 끝났다.

### 상태 코드 판정을 버린 게 아니라 얹었다

처음엔 "B 는 본문 코드로 판정한다"로만 생각했다. 짜면서 아니라는 걸 알았다 — **B 도 연결이 안
되거나 응답이 늦거나 인프라가 5xx 를 내면 그건 상태 코드로 온다.** 본문 코드 판정은 그 위에
얹히는 층이지 대체가 아니다.

`adapter.support` 가 "어느 공급사에게나 똑같이 생기는 실패"와 "공급사마다 다른 통지 방식"을
나눠 뒀는데, 사용자가 하나뿐이라 그건 아직 추측이었다. **두 번째 구현체가 그 선을 처음 검증했다.**

### Mock 에서 계약을 읽을 뻔했다

B 의 결과 코드를 사유별로 나누면서 **Mock 이 구현한 두 개(성공·장애)가 전부인 줄 알았다.**
"아는 게 둘뿐이니 나머지는 뭉갠다"까지 적었다가, 스펙을 다시 보고 여섯 개가 전부 정의되어
있다는 걸 확인하고 되돌렸다.

**Mock 은 검증 도구지 계약의 출처가 아니다.** 세 상황(정상/장애/무응답)을 재현하는 게 목적이라
코드를 전부 구현할 이유가 없었고, 그래서 좁다. 그걸 계약으로 읽으면 **Mock 이 안 만든 경우는
없는 것이 된다.**

되돌린 결과가 더 낫다. 잘못된 요청·인증 실패·호출 한도 초과가 각각 구분되므로 **A 의 상태 코드
판정과 같은 해상도**가 된다. 뭉갰다면 인증 실패가 "재시도 가능"으로 나가 클라이언트가 무의미한
재시도를 하고, 한도 초과에는 즉시 재시도를 걸어 상황을 악화시켰을 것이다. `api.md` 의 재시도
열은 통지 방식과 무관하게 성립해야 쓸모가 있다.

목록에 없는 코드만 `UNKNOWN` 으로 남긴다. 모르는 것을 아는 사유로 밀어 넣지 않는 것과, 알 수
있는 것을 안 나누는 것은 다르다.

### 묶음 상한도 같은 실수를 할 뻔했다

B 의 묶음 상한을 A 와 다르게 두고 "공급사마다 정하는 값"이라고 적었는데, 스펙에는 **둘 다 같은
값**으로 정해져 있다. 정해진 조건을 우리가 고른 것처럼 쓴 셈이라 Day 1 에 스택 설명에서 걷어낸
것과 같은 실수다. 값을 되돌리고 서술도 고쳤다.

값이 같아도 **검색 로직은 그 값이 같은지 알지 못한다.** 어댑터가 선언한 값에 맞춰 나눌 뿐이라
상한이 다른 공급사가 들어와도 고칠 곳이 없고, 그게 실제로 되는지는 상한 2 짜리 스텁으로 확인한다.
"다르게 뒀으니 검증된다"가 아니라 "달라도 되게 짜여 있다"가 맞는 말이었다.

### 부분 실패가 이제야 진짜 부분 실패다

공급사가 하나일 때는 "A 를 죽이면 결과 0 건"이었다. 그건 부분 실패가 아니라 **전부 실패**고,
`partial: true` 가 붙어 있어도 그 필드가 하는 일을 보여주지는 못했다. 이제 A 만 죽이면 B 결과가
그대로 나간다. E2E 테스트 이름도 그렇게 고쳤다.

### 세금 포함 총액이 아니면 상품을 만들지 않는다

B 는 `taxIncluded` 로 그 총액이 세금 포함인지 알려준다. 거짓이면 우리 표준(세금 포함 총액)을
만들 수 없다 — 세액을 안 주므로 더할 값이 없고, 추정해 더하는 건 지어내는 일이다. 오늘 요금
기간 규칙에서 내린 것과 같은 판단이다.

### B 에는 기간 규칙이 적용될 데이터가 없다

A 에 넣은 "총액은 요청 기간에 대해서만" 규칙이 B 에는 적용되지 않는다. 자를 날짜가 없기 때문이다.
뒤집어 말하면 **B 가 준 총액이 정말 요청한 기간에 대한 값인지 확인할 방법이 없다.** 믿는 수밖에
없는 지점이고, 표준 모델이 잃는 것 중 하나로 적어 둔다.

## Day 3 (이어서) — 두 어댑터가 계약을 다르게 지키고 있었다

### 전제가 틀렸다

"B 에는 있고 A 에는 없는 방어를 A 에 넣는다"로 시작했다. 코드를 열어 보니 **둘 다 구멍이 있었고
위치만 달랐다.**

| 예외가 빠져나가는 자리 | A | B |
| --- | --- | --- |
| `SupplierRoomType` — 수용 인원 1 미만 | 뚫림 | 뚫림 |
| `DailyInventory` — 음수 재고 | 뚫림 | 뚫림 |
| `Money` — 음수 금액·통화 3자리 아님 | 뚫림 | 막힘 |
| 본문이 값 없이 끝남 | 뚫림 | 막힘 |

앞의 둘은 **숙소 목록 조회 전체**와 **재고·요금 묶음 전체**를 죽이는 자리였다. 공급사가 수용
인원 0 인 객실 하나를 보내면 그 공급사 숙소가 전부 안 들어온다.

B 를 만들지 않았으면 이걸 못 봤을 것이다. **비교 대상이 생기니까 "같은 계약을 다르게 지키고
있다"가 보였고, 그러고 나서야 둘 다 틀렸다는 게 보였다.**

### 문서에 적어둔 문구가 부정확했다

`adding-supplier.md` 에 "본문이 비어 오면 값 없이 끝난다"고 적었는데, 실제로 재현해 보니 셋이
서로 다르게 동작했다.

| 본문 | 실제 |
| --- | --- |
| 빈 문자열 | 디코더가 오류로 잡는다 → `MALFORMED_RESPONSE` |
| `{}` | 정상적인 **빈 결과** |
| `null` | **값 없이 끝난다** ← 이것만 계약 위반 |

고쳐야 할 대상은 "빈 본문"이 아니라 **`null` 로 디코딩되는 본문**이었다. 임시 테스트로 셋을
찍어 보고 문서 문구를 고쳤다. **적어둔 문장을 그대로 믿고 고쳤으면 엉뚱한 것을 막을 뻔했다.**

### 두 겹으로 막았다

한 겹으로 하려다 나눴다.

- **아는 무효값**(수용 인원 1 미만, 음수 재고)은 루프에서 명시적으로 거른다 → **그 객실 타입,
  그 날짜만** 빠진다. 숙소나 상품을 통째로 버리지 않는다.
- **나머지**는 항목 경계에서 `IllegalArgumentException` 을 잡는다 → 그 항목만 빠진다.

앞만 두면 예상 못 한 값에 뚫리고, 뒤만 두면 객실 하나 때문에 숙소가 통째로 날아간다. 손실을
줄이는 것과 계약을 지키는 것이 다른 층이라 겹쳐 뒀다.

**`NullPointerException` 은 잡지 않는다.** 그건 공급사 데이터가 이상한 게 아니라 매퍼가 null
검사를 빠뜨린 우리 버그다. 값으로 삼켜 버리면 조용히 결과만 줄어든다.

### 코드는 오히려 줄었다

바깥 경계가 생기니 안쪽에 넣어 뒀던 `try/catch` 두 개가 필요 없어져 지웠다. B 의 `normalize` 에
있던 `body == null` 분기도 지웠다 — Reactor 가 null 을 내보내지 않으므로 도달할 수 없는 코드였고,
그 경우는 `defaultIfEmpty` 가 받는다. **방어를 옳은 자리 하나로 옮기면 자리마다 두던 방어가
없어진다.**

### 열린 문제 셋을 닫고 둘을 적었다

닫은 것은 매핑 예외가 `UNKNOWN` 으로 분류되던 것(그 경로 자체가 없어졌다), 항목 단위 방어가
요금 경로에만 있던 것, A 에 `defaultIfEmpty` 가 없던 것이다.

새로 적은 둘은 전체 점검에서 나왔지만 아직 안 고친 것이다 — 넓은 예외 핸들러, 결과 정렬
비결정성. **고칠 것과 알고 두는 것을 목록에서 구분해 둔다.**

### AI 활용

**수용** — 요금 규칙을 도메인 팩토리에 두자는 제안. 근거가 이 저장소가 이미 쓰고 있는 장치들과
같은 논리였고(규약 대신 컴파일러), `architecture.md` 가 "요금 정규화 규칙 변경 → `StayPrice` 의
두 팩토리"라고 이미 그 자리를 지정해 둔 것과도 맞았다.

**요구한 것** — 코드만 보고 "버그다"라고 하는 답을 그대로 받지 않고 **실제로 돌려서 숫자를
보이게 했다.** 440,000 을 눈으로 보기 전까지는 가설이다. 구현 직전에도 한 번 더 점검시켰고,
거기서 픽스처 충돌 두 건이 나왔다.

**남겨둔 것** — 같은 점검에서 나온 다른 지적들은 커밋을 나눠 처리했다. 묶음 크기 검증은 B 가
두 번째 상한을 들고 오므로 그 앞에 먼저 넣었고, 넓은 예외 핸들러와 결과 정렬 비결정성은 아직
남겨 뒀다. 한 커밋이 한 가지를 말하게 두는 편이 히스토리에 낫다.

**요구한 것 (B)** — 붙이기 전에 확인 기준을 먼저 정하게 했다. "도메인이 안 바뀌었는지"를
`git diff --stat` 으로 확인하고 그 결과를 그대로 적기로 했고, 0 줄이 아니었으면 그것도 적을
생각이었다. 결과가 좋아서 쓴 게 아니라, 확인할 방법을 먼저 정해 두고 쓴 것이다.

---

## Day 4 — 지표를 설계하려다 분류 버그를 찾았다

### "쓸지 말지"가 남은 일이었다

`observability.md` 를 미뤄둔 이유는 분명했다. `adding-supplier.md` 와 `integration-model.md` 는
**실제로 밟은 뒤에** 썼는데, 지표는 붙이지 않은 채 쓰면 "이렇게 될 것이다"가 된다. 그래서 README 에
"쓸지 말지를 정하는 것까지가 남은 일"로 적어뒀다.

쓰기로 하되 **방식을 갈랐다.** 현재 상태는 전부 실측으로 확인하고, 앞으로의 모양만 설계로 적는다.
문서 안에서 ✅ 와 ○ 로 표기를 나눈다. 이러면 검증되지 않은 문장이 검증된 문장과 같은 무게로 읽히는
일이 없다.

**갈라 놓고 보니 실측 쪽이 더 많이 나왔다.** 이건 예상하지 못했다.

### 셀 것을 찾다가 없는 것과 틀린 것이 보였다

무엇을 셀지 정하려면 **셀 것이 어디 있는지**를 먼저 봐야 한다. 그 과정이 그대로 코드 점검이 됐다.

| 찾은 것 | 어떻게 |
| --- | --- |
| 시간을 재는 코드가 운영 코드에 하나도 없다 | `nanoTime`·`Timer`·`elapsed` 를 grep 했더니 0건 |
| 성공 경로에 로그가 없다 (실패만 `log.warn`) | 로그 문장을 전부 뽑아 놓고 봤다 |
| `MeterRegistry` 가 클래스패스에 없다 | `micrometer-observation` 만 transitive 로 있고 `micrometer-core` 는 없다 |

앞의 둘이 뜻하는 것은 같다 — **보기로 한 세 지표 중 둘은 원천 데이터가 아예 없다.** 성공률은
분모가 없고, 응답 지연은 잴 것이 없다. "로그를 파싱하면 된다"도 안 된다. 실패만 남기 때문이다.

### 컴파일러가 먼저 알려줬다

세 번째 지표인 타임아웃 비율을 `reason=TIMEOUT` 으로 정의하려다, 그 값이 정말 타임아웃 전부를
담는지 확인했다. 임시 테스트에 `instanceof` 한 줄을 넣었더니 **컴파일이 안 됐다.**

```
error: incompatible types: ConnectTimeoutException cannot be converted to TimeoutException
```

`hasTimeoutCause` 가 보는 것은 `java.util.concurrent.TimeoutException` 과
`io.netty.handler.timeout.ReadTimeoutException` 둘인데, 연결 타임아웃이 던지는
`io.netty.channel.ConnectTimeoutException` 은 **`java.net.ConnectException` 을 상속하는 무관한
타입**이었다.

거기서 멈추지 않고 닿지 않는 주소(`10.255.255.1`)로 실제 호출해 확인했다. Day 3 에 요금 버그를
440,000 이라는 숫자로 확인한 것과 같은 이유다 — **컴파일러가 맞다고 해도 실행 경로가 그리 간다는
보장은 아니다.**

```
cause: io.netty.channel.ConnectTimeoutException / connection timed out after 1000 ms
걸린 시간 = 1104ms
분류      = SUPPLIER_ERROR
```

### 왜 이게 지표만의 문제가 아닌가

`api.md` 는 `SUPPLIER_ERROR` 를 "공급사가 자기 쪽 오류를 알림"으로 정의했다. 그런데 연결
타임아웃은 **공급사가 아무것도 알린 적이 없는** 경우다. 지표 이전에 **응답의 `reason` 값이 지금
틀리다.**

증상도 정반대로 읽힌다. 응답이 아예 없는 것은 네트워크 경로를 의심할 일인데, "공급사가 자기 오류를
알렸다"로 나가면 공급사에 문의하게 된다.

그리고 README §타임아웃 은 연결 제한 2초를 **근거를 들어 정한 값**으로 내세운다. 그 값이 동작한
결과가 정작 **그것을 보려고 만들 지표에 안 나타난다.**

### 왜 그동안 안 보였나

테스트가 응답 타임아웃(Mock 무응답 모드)만 확인하고 있었다. `TIMEOUT` 을 기대하는 테스트 파일이
여덟인데 **전부 응답 쪽이거나 손으로 만든 실패 값**이고, 연결 타임아웃 분류를 확인하는 것은 하나도
없었다.

Day 3 에 적은 것과 같은 모양이다 — **검증하는 쪽이 같은 구멍을 갖고 있으면 잡을 수 없다.** 그때는
테스트 픽스처가 새 규칙과 부딪혔고, 이번엔 테스트가 한쪽 경로만 지나가고 있었다.

### 고치지 않고 열린 문제로 옮겼다

`HttpFailures` 에 타입 하나를 더하면 끝나는 수정이다. 그래도 이 커밋에서 하지 않았다. **응답의
`reason` 값이 바뀌는 동작 변경**이고, 그 값을 기대하는 테스트가 여럿이라 문서 커밋에 섞을 일이
아니다. 한 커밋이 한 가지를 말하게 두는 편이 낫다(Day 3 에서 같은 이유로 넓은 예외 핸들러를
남겨뒀다).

### 설정값만으로 계산되는 문턱이 있었다

세 상한(연결 2s · 응답 3s · 검색 전체 5s)이 독립이 아니라는 걸 재조정 절차를 적다가 알았다.
한 공급사의 최악 소요는 `ceil(묶음 수 / max-concurrency) × 응답 제한` 이다.

기본값에 넣으면 숙소 200개까지는 묶음 4개라 한 파동(3초)인데, **201개부터 묶음이 5개가 되어 두
파동(6초)**이 되고 검색 전체 상한 5초를 넘는다.

이건 관측이 필요한 값이 아니라 **설정만으로 나오는 수인데 그동안 아무도 계산하지 않았다.** README
§숙소가 수천 개가 되면 이 "이 규모에서는 한계"라고만 적어둔 것의 실제 시작점이다. 수천 개가 아니라
**200개**였다.

### AI 활용

**요구한 것** — 문서를 쓰기 전에 **주장할 내용을 먼저 검증하게 했다.** "지표를 이렇게 붙이면
됩니다"를 받아 적었으면 위 셋(시간 측정 부재, 성공 로그 부재, 연결 타임아웃 오분류)이 전부 안
나왔다. 지표 문서는 코드에 무엇이 있는지에 전적으로 의존하는데, 그 확인 없이 쓰면 그럴듯한 목록만
남는다.

**요구한 것 (2)** — 연결 타임아웃 건은 컴파일 오류만으로도 충분해 보였지만 실제로 돌려서 숫자를
보게 했다. Day 3 의 440,000 과 같다. **1104ms 와 `SUPPLIER_ERROR` 를 눈으로 보기 전까지는
가설이다.**

**거부** — 지표를 각 어댑터 안에 넣자는 안을 물리고 데코레이터로 갔다. 어댑터마다 같은 계측을
되풀이하면 `adding-supplier.md` 절차에 항목이 하나 늘고, 새 공급사가 빠뜨려도 아무것도 잡아주지
않는다. **경계를 지키는 방식이 이 저장소에서 이미 정해져 있는데** 지표만 예외로 둘 이유가 없었다.

**수정** — 초안이 지표 라벨에 `detail` 을 넣고 있었다. 예외 메시지가 그대로 들어가는 값이라 시계열이
무한정 늘어난다. 라벨은 가짓수가 미리 정해진 것만 넣는 것으로 고치고, 문서에 안 넣는 것 목록을 따로
뒀다.

## Day 4 (이어서) — 목록이 두 곳에 있던 것이 뿌리였다

### 문서가 맞고 코드가 틀린 경우였다

지표 문서를 쓰며 찾은 연결 타임아웃 오분류를 고쳤다. 고치기 전에 `api.md` 를 다시 봤는데,
`TIMEOUT` 이 이미 **"제때 응답하지 않음 (연결·응답·검색 전체 상한)"** 으로 정의되어 있었다.

**이번엔 문서를 고칠 게 없었다.** 지금까지 문서–코드 불일치는 대개 문서가 앞서 나간 쪽이었는데
(Day 1 의 "H2 가 구성된 것처럼 적힌 표"), 이번은 반대였다. 문서가 약속한 동작을 코드가 안 하고
있었고, 그 약속이 옳았다. **그래서 코드를 문서에 맞췄다.**

### 타입 하나 더하고 끝낼 수 있었지만

`hasTimeoutCause` 에 `ConnectTimeoutException` 한 줄을 더하면 증상은 사라진다. 그렇게 하지 않은
이유는 **왜 빠졌는지**가 따로 있었기 때문이다.

타임아웃 판정 목록이 두 곳에 있었다 — `fromThrowable` 의 `switch` 에 두 개, `hasTimeoutCause` 에
같은 두 개. 한쪽에만 타입을 더하면 **감싸여 온 경우와 그대로 온 경우가 다르게 분류된다.** 지금
고쳐도 다음 타입에서 같은 일이 난다.

판정을 `isTimeout` 하나로 모으고, `switch` 는 그 사슬 탐색을 그대로 쓰게 했다. 이러면 **감싸여
왔든 그대로 왔든 한 규칙**이다. `ApiErrorHandler` 에 적어둔 것과 같은 말이다 — 규칙이 한 곳에만
있어야 두 곳이 어긋나지 않는다.

결과적으로 `switch` 의 타임아웃 분기 두 개가 하나로 줄었다.

### 거절까지 휩쓸 뻔했다

`ConnectTimeoutException` 이 `ConnectException` 을 상속한다는 걸 알고 나니 `ConnectException` 을
통째로 타임아웃으로 보는 쪽이 간단해 보였다. **그러면 연결 거절이 함께 딸려 온다.**

둘은 다른 사건이다. 거절은 상대가 아예 안 떠 있다는 뜻이고 **기다리지도 않고** 즉시 실패한다.
타임아웃은 제한 시간을 다 쓰고 끊은 것이다. `api.md` 재시도 열에서도 판단이 갈릴 수 있는 자리라
가르는 쪽이 맞다. 테스트에 거절 케이스를 같이 넣어 이 구분이 깨지면 빨간불이 켜지게 했다.

### 테스트를 어디에 둘지

`adapter.support` 에 테스트가 하나도 없었다. 두 어댑터가 공유하는 코드인데 검증은 어댑터 테스트에
간접적으로만 얹혀 있었고, 그래서 **아무도 안 보는 구간(연결)이 생겼다.**

어댑터 테스트에 나눠 넣는 안은 버렸다. 같은 규칙을 공급사 수만큼 되풀이하게 되고, 공급사가 늘 때
한쪽만 고치는 일이 생긴다. `HttpFailuresTest` 를 새로 두고 **구간 셋을 한자리에서** 고정했다.

계층 관계를 확인하는 테스트도 넣었다. `ConnectTimeoutException` 이 다른 두 타임아웃 타입과 무관
하다는 사실이 **왜 이걸 따로 적어야 하는지**의 이유 전부라, 그게 테스트로 남아 있어야 다음 사람이
"이름이 비슷하니 묶여 있겠지" 하고 지우지 않는다.

### 실패하는 것을 먼저 봤다

테스트를 짜고 통과하는 것만 보면 **그게 이 버그를 잡는지 알 수 없다.** 운영 코드만 `git stash` 로
되돌리고 다시 돌려서 **연결 관련 두 건이 실패하고 나머지 열 건이 통과하는 것**을 확인했다.

열 건이 통과한다는 쪽도 같이 필요했다. 그게 이번 수정이 다른 분류를 안 건드렸다는 뜻이다.

### 실제 연결 타임아웃 통합 테스트는 안 넣었다

Mock 무응답 모드가 응답 타임아웃을 실제 전송 계층으로 확인하듯이, 연결 타임아웃도 그렇게 하고
싶었다. **재현하려면 SYN 을 삼키는 주소가 필요한데 그건 환경에 따라 달라진다.** 확인할 때 쓴
`10.255.255.1` 도 망 구성에 따라 즉시 거절이 올 수 있다. 환경 따라 결과가 바뀌는 테스트는
빨간불의 뜻이 흐려져서 없느니만 못하다.

단위 테스트가 분류를 고정하고, 전송 계층을 실제로 지나가는 확인은 응답 타임아웃 쪽이 이미 하고
있다. **못 하는 것을 아는 채로 두는 것과 안 하는 것은 다르므로** 여기 적어 둔다.

### 세어 보니 문서가 맞았다

README 의 "테스트 124개"가 실제와 어긋난 줄 알았는데, 세는 방법이 틀렸던 쪽은 나였다.
`@Test` 어노테이션을 grep 하면 126 이 나오지만 **실제 실행 건수는 124** 다. 지금은 12 건이 늘어
136 이다.

문서를 고치기 전에 **어떻게 센 숫자인지부터** 봤어야 했다. 어긋났다고 판단한 근거가 다른 척도였다.

### AI 활용

**요구한 것** — 고치기 전에 회귀 위험을 먼저 훑게 했다. "연결 실패 → `SUPPLIER_ERROR`" 를 기대하는
테스트가 있으면 이 수정이 그걸 깨뜨린다. 확인해 보니 없었고, `SyncFailureDoesNotBlockStartupTest`
가 쓰는 `localhost:1` 은 **닫힌 포트라 거절이지 타임아웃이 아니라서** 영향이 없었다. 이 확인을
안 했으면 전체 테스트를 돌리고 나서야 알았을 것이다.

**요구한 것 (2)** — 새 테스트가 정말 이 버그를 잡는지 **수정을 되돌려서** 보이게 했다. 통과하는
테스트는 아무것도 증명하지 않는다.

**거부** — `ConnectException` 전체를 타임아웃으로 보자는 안. 간단하지만 연결 거절이 함께 딸려 온다.

**수정** — 초안이 `hasTimeoutCause` 에 타입만 한 줄 더하고 끝내려 했다. 증상은 사라지지만 목록이
두 곳에 남아 다음 타입에서 같은 일이 난다. 판정을 한 자리로 모으는 쪽으로 바꿨다.
