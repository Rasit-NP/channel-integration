# 진행 기록

**무엇을 정했는지는 [README](README.md) 에 있다. 여기에는 그 결론에 이르는 동안 무엇이
바뀌었는지만 적는다.** 처음 생각과 끝이 같은 결정은 README 만 보면 되므로 여기 반복하지 않는다.
뒤집은 판단, 버린 대안, 잘못 짚었다가 고친 것이 이 문서의 내용이다.

## 요약

| 날짜 | 한 일 | 커밋 |
| --- | --- | --- |
| Day 1 | 프로젝트 세팅, 통합 모델 설계 확정, Mock 구성, 표준 모델·연동 경계, 공급사 A 어댑터, 문서 역할 정리 | `a823d4f` … `9a97a64` |
| Day 2 | 남은 작업 순서 정리, 묶음 초과를 실패 값으로 정정, 매핑 저장 (H2 · JdbcClient) | |

## 열린 문제

해결되면 지운다. 날짜별로 쌓지 않는다.

| # | 문제 | 방향 |
| --- | --- | --- |
| 1 | 매핑 단계 예외가 `MALFORMED_RESPONSE` 가 아니라 `UNKNOWN` 으로 분류된다. `HttpFailures.fromThrowable` 이 `CodecException` 만 잡고 나머지를 default 로 흘린다. 임시 테스트로 확인했다 | "우리 변환이 못 따라간 것"과 "원인 불명"은 대응이 다르다. 매퍼에서 잡아 사유를 붙이거나 `fromThrowable` 에서 구분한다 |
| 2 | README 에 `supplier.a.*` 설정 키 설명이 없다 | 검색 API 를 붙일 때 실행 절에 함께 적는다 |
| 3 | macOS 기동 시 Netty DNS 리졸버 경고가 ERROR 레벨로 찍힌다. localhost 만 쓰므로 기능엔 영향 없다 | 거슬리면 `netty-resolver-dns-native-macos` 를 개발 의존성으로 추가 |
| 4 | 무응답 모드 테스트가 끝나도 Mock 의 스레드가 10분간 붙잡혀 있어, 컨텍스트 종료가 30초 대기한 뒤 강제로 끊긴다. 빌드가 그만큼 길어진다 | 붙잡는 시간을 타임아웃보다 조금만 길게 줄이거나, 모드 복구 시 깨우는 방식으로 바꾼다 |

---

## Day 1 — 프로젝트 세팅부터 공급사 A 어댑터까지

### 한 일

- Spring Boot 프로젝트 스캐폴딩, 의존성 정리
- 통합 상품 모델·재고 판정·부분 실패 표현 설계 확정 → README 작성
- Mock Supplier 구성 (별도 포트 9090, 정상/장애/무응답 3모드)
- 표준 모델(domain)과 공급사 연동 경계(port) 정의
- 공급사 A 어댑터 — WebClient 연동, 실패 정규화, 타임아웃. 테스트 37개

주어진 조건(Java 21, Gradle, Spring Boot, WebClient, 관계형 DB)은 고른 게 아니라 받은 것이므로
판단으로 적지 않는다.

### 판단이 바뀐 지점

**요금 표준을 날짜별 단가로 잡으려다 총액으로 뒤집었다** → README [§표준 요금](README.md)

처음엔 정보량이 많은 쪽이 상위 호환이라고 봤다. 뒤집은 계기는 **변환이 한 방향으로만 가능하다**는
걸 확인한 것이다. 날짜별을 합쳐 총액을 만들 수는 있어도, 총액을 날짜별로 쪼개려면 날짜 수로
나누는 수밖에 없고 그건 없는 값을 지어내는 일이다. 정보를 잃는 것보다 지어내는 것이 나쁘다는
기준이 이때 생겼고, 이후 판단에도 계속 썼다.

**포트 시그니처를 값에서 `Mono` 로 되돌렸다**

`SupplierAdapter` 가 결과를 바로 반환하도록 정의하고 **커밋까지 했다.** A 어댑터를 붙이기 직전에
뒤집었다. 값을 바로 돌려주면 여러 공급사를 동시에 호출하는 데 별도 스레드 풀이 필요하고,
응답을 기다리는 동안 스레드가 묶인다. 동시에 기다리는 시간이 이 시스템의 부하인데 그걸 스레드로
감당하는 셈이다.

구현체가 하나도 없던 시점이라 비용이 거의 없었다. **인터페이스는 구현이 붙기 전에 의심해 보는
게 싸다.**

**어댑터가 어디까지 책임질지 좁혔다**

재고 판정과 내부 식별자 매핑까지 어댑터에 넣으려다 뺐다. 재고 판정은 요청 기간을 알아야 하고
규칙도 공급사가 아니라 우리 정책이다. 매핑은 DB 가 필요하다. 어댑터는 표현의 **형태**만 통일하고
**정체**는 application 이 확정하는 것으로 갈랐다.

50개 묶음도 같은 기준으로 나눴다. 한계는 공급사의 제약이니 어댑터가 `maxBatchSize()` 로 선언만
하고, 나누는 일은 application 이 한다. 한계가 다른 공급사가 들어와도 검색 로직은 그대로다.

**테스트를 한 층만 쓰려다 두 층으로 나눴다**

응답을 갈아끼우는 단위 테스트면 충분하다고 봤는데, 그 방식은 전송 계층을 지나가지 않는다.
Netty 타임아웃이 실제로 걸리는지, 직렬화 경로가 맞는지는 확인되지 않고 "내가 짠 대로 동작한다"만
확인된다. Mock 을 실제로 띄우는 통합 테스트를 따로 뒀다.

**Mock 구성에 시간을 쓰려다 멈췄다**

WireMock 이냐 별도 모듈이냐를 고민했는데, Mock 은 결과물의 일부가 아니고 복사해 쓸 수 있는
뼈대가 있었다. 별도 포트로 띄우는 것만 지키고 나머지는 손대지 않았다. 요청 파라미터도 무시한다.
**필요한 건 데이터 다양성이 아니라 정상/장애/무응답 세 상황의 재현이다.**

### 그대로 간 결정

처음 판단이 끝까지 유지된 것들이다. 근거는 README 에 있다.

- 조식 포함 여부를 요금과 같은 층위의 표준 필드로 → README §조건이 다른 상품
- 중복 상품 병합하지 않음 (공통 키가 없어 추정에 의존해야 함) → README §동일 숙소를 병합하지 않는 이유
- 연박 재고는 날짜별 최솟값, 0 이면 응답에서 제외 → README §재고 판정
- 실패를 예외가 아니라 값으로. 부분 실패를 허용하려면 성공과 실패를 한 자료구조에 담아야 한다

### 막힌 것

**Initializr 가 3.5.x 를 제공하지 않았다.** 4.x 만 목록에 있어 4.1.1 로 받은 뒤 버전을 내렸다.
로컬에 gradle CLI 가 없어 wrapper 를 직접 만들 수 없었기 때문에 Initializr 를 거치는 게 빨랐다.

**두 모듈의 `application.yaml` 이 클래스패스 루트에서 충돌할 뻔했다.** Mock 을 테스트 의존성으로
넣으면서 발견했다. 어느 쪽이 읽힐지가 클래스패스 순서에 좌우된다. 지금은 메인 것이 먼저 잡혀
문제가 없었겠지만 조용히 깨질 종류라, Mock 설정을 기본 프로퍼티로 옮기고 파일을 없앴다.

**문서에 스택 선택의 근거를 길게 적었다가 전부 걷어냈다.** 주어진 조건을 고른 것처럼 쓴 셈이라
읽는 사람을 오해시킨다. 진짜 판단이 있었던 자리만 남겼다.

**커밋을 어떻게 나눌지 고민했다.** 한 덩어리로 커밋하면 어디까지가 스캐폴딩이고 어디부터 내가
만든 것인지 구분되지 않는다. 교체 전후를 각각 커밋해 diff 로 남기는 안도 검토했는데, 되돌렸다
다시 바꾸는 커밋을 인위적으로 만드는 셈이라 접었다.

### AI 활용

Claude Code 를 붙여 진행한다. 저장소에 `CLAUDE.md` 를 두고 기술 스택·아키텍처 원칙·저장 정책·
공개 저장소 주의사항을 미리 지시해, 매번 배경을 다시 설명하지 않아도 되게 했다.

**수용** — 요금 표준을 잡을 때 "변환이 한 방향으로만 가능하다"는 정리. 내가 날짜별을 표준으로
잡으려던 걸 뒤집는 근거가 됐다. `spring.main.web-application-type: servlet` 으로 웹 스택을
고정하자는 제안도 받아들였다. 문서에만 적어둔 결정은 지켜지지 않는다는 지적이 타당했다.
어댑터보다 Mock 을 먼저 만들자는 순서 제안도 마찬가지다 — 고장을 재현할 상대가 없으면 견고성
코드를 짜도 확인할 수 없다.

**수정** — 초기 `.gitignore` 주석에 이 저장소에 들어가면 안 되는 표현이 섞여 있었다. 푸시 전이라
히스토리를 다시 써서 지웠다. README 초안이 존재하지 않는 `docs/` 문서로 링크를 걸어둬서
"문서 계획" 표로 바꿨다.

**거부 / 잡아낸 것** — README 초안의 기술 스택 표에 `Database | H2` 가 실제 구성처럼 적혀 있었다.
확인해보니 `build.gradle.kts` 에 H2 도 JPA 도 JDBC 도 없었다. 다른 줄은 전부 실재하는 항목이라
H2 도 구성된 것처럼 읽히는 상태였다. **AI 가 쓴 문서를 그대로 받으면 이런 게 남는다.** 같은
검증에서 두 개를 더 잡았다 — 재고 0 을 제외한다고 써놓고 응답 예시에 제외 건수 필드가 없던 것,
"8080 에서 서비스합니다"라고 적었지만 실제로는 404 가 나는 것.

마감 점검에서도 구현 현황 표가 실제와 어긋난 걸 찾았다. 도메인 모델과 A 어댑터를 만들고도 표는
미구현으로 남아 있었다. **문서와 코드를 맞추겠다고 적어둔 표에서 같은 실수가 반복된다.** 작업
끝에 표를 다시 보는 것을 습관으로 삼는다.

### 다음

1. 매핑 저장(H2) + 검색 유스케이스 — A 만으로 수직 슬라이스를 끝까지 관통시킨다. B 는 A 와
   구조가 같아 붙는 게 거의 확실한 반면, 매핑 저장과 검색 조립에는 아직 드러나지 않은 문제가
   있을 수 있다
2. 공급사 B 어댑터. 도메인을 안 고치고 붙는지가 경계 격리의 시험대다. B 는 일부러 까다롭다 —
   장애에도 HTTP 200, 총액만 제공, 날짜별 요금 없음
3. 병렬 호출과 부분 실패 병합. 묶음 초과가 예외로 나가는 문제를 여기서 함께 정리한다
4. `docs/adding-supplier.md` — B 를 붙이면서 실제로 고친 것을 그대로 적는다

---

## Day 2 — 묶음 초과를 예외에서 실패 값으로

### 판단이 바뀐 지점

**`Mono.error` 로 돌리려다 실패 값으로 갔다**

묶음 크기를 넘겼을 때 `SupplierAAdapter.fetchOffers` 가 `Mono` 를 만들기도 전에 예외를 던지고
있었다. 병렬 병합을 붙이면 한 공급사가 아니라 merge 전체가 무너지는 자리라, 열린 문제로 적어두고
`Mono.error` 로 바꿀 생각이었다.

바꾸려고 보니 `Mono.error` 도 반쪽이었다. 던지는 위치만 옮길 뿐 여전히 예외 채널로 나가고,
받는 쪽은 `onErrorResume` 으로 다시 값으로 되돌려야 한다. 포트가 "어떤 경우에도
`SupplierFetchResult` 를 돌려준다"고 선언해 둔 것과도 어긋난다. `FailureReason.INVALID_REQUEST`
주석에 "묶음 크기 초과 등"이라고 이미 적어둔 걸 보고, 자리가 처음부터 있었다는 걸 알았다.

**호출한 적도 없는데 실패로 치는 게 맞나**를 잠깐 의심했다. 맞다고 봤다. 실패 사유를 "공급사가
무엇을 돌려줬나"가 아니라 **"이 조회가 왜 결과를 못 냈나"**로 잡았기 때문이다. 잘못된 요청이라
보내지 않은 것도 그 조회가 결과를 못 낸 이유다. 응답에 드러나야 하는 것도 같다 — 고객 입장에서는
공급사가 죽은 것이나 우리가 잘못 물어본 것이나 **그 공급사 결과가 빠졌다**는 점에서 다르지 않다.

테스트도 함께 뒤집었다. 예외를 기대하던 것을 실패 사유를 확인하는 것으로 바꾸고, 호출하면
실패하는 `ExchangeFunction` 을 넣어 **정말로 호출하지 않는지**까지 같이 본다. 값으로 바꾸면서
"막았다"와 "불러봤다 실패했다"가 겉보기에 같아졌기 때문에, 그 구분은 테스트가 잡아야 한다.

### 미뤄둔 결정을 열어보니 정해진 게 아니었다

JPA 냐 JDBC 냐를 Day 1 에 미뤄두면서 "스키마를 정하면서 함께 결정한다"는 조건을 걸어뒀다.
꺼내기 전에 **주어진 조건에 정해진 게 있는지부터 확인했다.** 관계형 DB 를 쓴다는 것까지만
정해져 있고 접근 기술은 열려 있었다.

HTTP Client 가 WebClient 로 못박혀 있는 것과 대비된다. **못박을 항목은 못박혀 있다는 뜻이고,
DB 접근 방식이 열려 있는 건 우리가 정하라는 것으로 읽었다.** 그래서 고르는 것 자체보다 고른
이유를 남기는 쪽이 맞다고 보고 진행했다 → README [§무엇으로 저장하는가](README.md)

미뤄둔 조건도 결과적으로 맞았다. 스키마를 실제로 쓰기 전에는 "테이블 둘에 관계가 없다"가
얼마나 결정적인지 몰랐다. 그게 보이고 나니 ORM 을 얹을 이유가 남지 않았다.

### 스키마를 쓰다가 새로 생긴 질문

이름과 최대 수용 인원을 매핑 행에 같이 둘지 — 설계할 때는 나오지 않았던 질문이다. 컬럼을
적다가 걸렸다. 안 넣기로 했다 → README [§이름을 저장하지 않는 이유](README.md)

넣는 쪽 논거도 있었다. 공급사가 죽어 있어도 우리가 아는 숙소 목록을 보여줄 수 있고, 공급사
응답과 대조해 변경을 감지할 여지도 생긴다. 그런데 그 둘 다 **지금 없는 기능**이고, 대신
같은 값의 출처가 둘이 되는 문제는 **지금 생긴다.** 나중에 필요해지면 컬럼을 추가하는 쪽이,
지금부터 어긋남을 관리하는 것보다 싸다.

이건 "정보를 잃는 것보다 지어내는 것이 나쁘다"(Day 1)와는 다른 축이다. 여기서 잃는 건 없다.
**같은 사실을 두 곳에 두지 않는다**는 쪽에 가깝다.

### 테스트가 무엇을 지키는지

upsert 가 도는지가 아니라 **내부 식별자가 안정적인지**를 본다. 같은 목록을 두 번 넣고 식별자를
비교하는 테스트가 이 저장소의 존재 이유를 그대로 검사한다.

unique 제약이 실재하는지도 따로 확인한다. 식별자 안정성을 보장하는 것은 upsert 문장이 아니라
제약인데, `MERGE` 만 통과시켜 보면 제약이 빠져 있어도 테스트가 통과한다. 보장 장치라고 문서에
써둔 것은 그것이 있는지를 직접 봐야 한다.

### AI 활용

**수용** — 남은 작업 순서를 짜면서, 이 수정을 매핑 저장보다 먼저 두자는 제안을 받아들였다.
병렬 병합을 붙인 뒤에 고치면 merge 가 무너지는 걸 디버깅하다 찾게 된다. 5분짜리를 앞으로
당기는 게 낫다.

**수정** — 열린 문제에 적어둔 `Mono.error` 를 그대로 하려다 위 이유로 실패 값으로 바꿨다.
문서–코드 대조에서 세 건을 받았고(포트 계약과 어긋나는 `throw`, 없는 `docs/` 디렉토리,
Day 1 커밋 범위 누락) 셋 다 실재해서 반영했다.

**되물어서 얻은 것** — JPA/JDBC 선택지를 정리해 준 답에 곧바로 고르지 않고 **"주어진 조건에
정해진 게 있느냐"를 먼저 물었다.** 없다는 것을 확인하고 나서야 고르는 게 순서였다. 정해진 것을
내 판단으로 덮어쓰는 일과, 열려 있는 것을 내가 정하는 일은 남겨야 할 기록이 다르다. 답을 받고
바로 고르면 그 확인이 통째로 빠진다.
